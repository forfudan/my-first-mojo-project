name: Unit Tests

on:
  push:
    branches: [main, dev]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  test:
    name: "Test: ${{ matrix.folder }}"
    runs-on: macos-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        folder: [start, move, basic, advanced, extend]
    steps:
      - uses: actions/checkout@v4

      - uses: prefix-dev/setup-pixi@v0.9.5

      - name: Show environment
        run: |
          eval "$(pixi shell-hook --shell bash)"
          echo "Mojo:   $(mojo --version)"
          echo "Python: $(python --version)"

      # verify.sh is the same script `pixi run verify` uses locally, so the
      # expected-failure / expected-warning / skip lists cannot drift apart.
      - name: "Verify src/${{ matrix.folder }}"
        run: |
          eval "$(pixi shell-hook --shell bash)"
          bash verify.sh "src/${{ matrix.folder }}"
